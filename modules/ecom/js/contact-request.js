// Boton request, para manejar las solicitudes de amistad.

$(".btn-contact").click(function(event){
  var boton = $(this);

  if(boton.attr("data-request-estatus") == "0" && boton.attr("data-request-estatus") == "1"){
    // Confirmar una solicitud de amistad
  }

  
}); 